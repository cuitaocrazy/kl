<ehcache>

    <!--
        磁盘存储:指定一个文件目录，当EHCache把数据写到硬盘上时，将把数据写到这个文件目录下
        path:指定在硬盘上存储对象的路径
        path可以配置的目录有：
            user.home（用户的家目录）
            user.dir（用户当前的工作目录）
            java.io.tmpdir（默认的临时目录）
            ehcache.disk.store.dir（ehcache的配置目录）
            绝对路径（如：d:\\ehcache）
        直接配置.cache会在当前程序目录生成一个.cache的目录，需要在ignore文件中添加忽略
     -->
    <!--    <diskStore path=".caches"/>-->

    <!--
        defaultCache:默认的缓存配置信息,如果不加特殊说明,则所有对象按照此配置项处理
        maxElementsInMemory:设置了缓存的上限,最多存储多少个记录对象
        eternal:代表对象是否永不过期 (指定true则下面两项配置需为0无限期)
        timeToIdleSeconds:最大的空闲时间 /秒
        timeToLiveSeconds:最大的存活时间 /秒
        overflowToDisk:是否允许对象被写入到磁盘,如果配置true则需要配置diskStore属性
        说明：下列配置自缓存建立起600秒(10分钟)有效 。
        在有效的600秒(10分钟)内，如果连续120秒(2分钟)未访问缓存，则缓存失效。
        就算有访问，也只会存活600秒。
     -->

    <!-- 大字段缓存 -->
    <cache name="msgContextCache" maxElementsInMemory="1000" eternal="false"
           timeToIdleSeconds="1" timeToLiveSeconds="1" overflowToDisk="false"/>

    <!-- 商户筛选条件缓存 -->
    <cache name="merQueryCache" maxElementsInMemory="1000" eternal="false"
           timeToIdleSeconds="1" timeToLiveSeconds="1" overflowToDisk="false"/>

</ehcache>